export type TwitterContent = {
  id: string;
  name: string;
  created_at: string;
  profile_image_url: string;
  text: string;
  username: string;
};

export type TwitterContents = TwitterContent[];
